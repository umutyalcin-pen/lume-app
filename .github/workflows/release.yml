name: Release Lume binaries

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    name: Build and Upload Release Binaries
    runs-on: windows-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.25.7'

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Build Python GUI version
        run: |
          python -m pip install --upgrade pip
          python -m pip install pyinstaller
          python -m pip install -r "python version lume\requirements.txt"
          cd "python version lume"
          pyinstaller --noconfirm --onefile --windowed --name Lume --icon lume.ico --add-data "lume.png;." --add-data "lume.ico;." main.py

      - name: Build Go GUI version
        run: |
          cd "go version lume"
          go build -ldflags="-H windowsgui" -o Lume_Pro.exe .

      - name: Build CLI version
        run: |
          cd "cli version lume"
          go build -o lume.cli.version.compact.exe .

      - name: Upload Binaries to GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            python version lume/dist/Lume.exe
            go version lume/Lume_Pro.exe
            cli version lume/lume.cli.version.compact.exe
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
